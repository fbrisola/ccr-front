import { Component } from '@angular/core';

@Component({
    moduleId: module.id,
    selector: 'people',
    templateUrl: 'people.component.html',
})

export class PeopleComponent {
    title = 'Membros';
}