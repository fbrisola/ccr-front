import { Component } from '@angular/core';

@Component({
    moduleId: module.id,
    selector: 'person',
    templateUrl: 'person.component.html',
})

export class PersonComponent {
    title = 'Membros';
}